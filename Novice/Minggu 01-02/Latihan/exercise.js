function min(x,y) {
     if (x<y) {
        return x;
      } else {
           return y;
      }
    }
document.write(min(0,10)); // Nilai minimum dari (0,10)
document.write(min(0, -10)); // Nilai minimum dari (0,-10)